import "../css/Logo.css";

// The site mark is a small organic aurora blob (matches the hero orb).
const Logo = () => {
    return <span className="logo" aria-hidden="true" />;
};

export default Logo;
